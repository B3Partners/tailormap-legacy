import { AttributelistFilter } from './attributelist-filter';

describe('AttributelistFilter', () => {
  it('should create an instance', () => {
    expect(new AttributelistFilter()).toBeTruthy();
  });
});
