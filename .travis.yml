language: java

sudo: false

install:
  # install without testing
  - mvn install -f viewer-config-persistence/pom.xml -B -V -fae -q
  - mvn install -Dmaven.test.skip=true -B -V -fae -q

script:
  # execute unit and integration tests
  - mvn -e test verify -B

jdk:
  - openjdk7
  - oraclejdk7
  - oraclejdk8

os:
  - linux
  # - osx

matrix:
  fast_finish: true

cache:
  directories:
  - $HOME/.m2
  
