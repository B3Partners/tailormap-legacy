Voor elke join table met order column, verwijder unique constraint (staat in create table)

Lijst van tabellen waarvoor dit geldt:

category_children (child)
category_services (service)
feature_source_feature_types (feature_type)
layer_children (child)
feature_type_attribute (attribute_descriptor)
